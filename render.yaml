services:
  - type: web
    name: internship-discord-bot
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    # Note: Add your environment variables in the Render dashboard
    # DISCORD_TOKEN, DISCORD_CHANNEL_ID, etc.

